<% title_actions link_to(_("New Realm"), new_realm_path) %>
<% title _("Realm Configuration") %>
<div id="welcome">
  <p>
  <%= _("Satellite 6 supports automatically creating realm entries for new hosts.  When a realm is selected for a host, Satellite 6 contacts the relevant realm Capsule to create an entry for the host and retrieve it's one-time registration password.").html_safe %>
  </p>
</div>
